# Quick Tmux Reload
unbind r
bind r source-file ~/.tmux.conf

# Changes global prefix from ctrl-b to Ctrl-s
set -g prefix C-s

# Add mouse to tmux
set -g mouse on

# hjkl movement
bind-key h select-pane -L


